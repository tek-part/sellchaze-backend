<div class="card-body py-3">
    <div class="d-flex flex-wrap gap-2 align-items-center">
        <a href="#" id="kt_notifications_reload" class="btn btn-sm btn-light-primary" title="{{ __('Reload') }}">
            <i class="iconoir-refresh"></i> {{ __('Reload') }}
        </a>
        <a href="#" id="mark-all" class="btn btn-sm btn-light-primary" title="{{ __('Mark all as read') }}">
            <i class="iconoir-check"></i> {{ __('Mark all as read') }}
        </a>
    </div>
</div>
